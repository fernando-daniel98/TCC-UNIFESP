# Configurações

Contém arquivos de configuração centralizados para o sistema.

## Arquivos

- `config.yaml` - Configuração principal do projeto
- `logging_config.yaml` - Configuração de logs
- `credentials.env.example` - Template para variáveis de ambiente sensíveis

